@model IEnumerable<practice.Models.TeamPlayer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlayerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JesrsyNo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlayerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JesrsyNo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlayerNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlayerNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlayerNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
